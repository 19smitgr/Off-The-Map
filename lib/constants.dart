import 'package:flutter/material.dart';

/// used throughout the app for the AppBars
const kDarkBlueBackground = Color(0xFF30465C);

/// originally used in `./views/partials/navigation_bar.dart`, but I don't think this is used now
const kLightBlueBackground = Color(0xFF3F6579);

/// used as the background color for most pages in the app
const kGrayBackgroundColor = Color(0xFFEDEDED);

const kHeader = TextStyle(
  fontWeight: FontWeight.w500,
  fontSize: 20.0,
);

const kAssignmentOptionStyle =
    TextStyle(color: Color(0xFF0A00FF), fontWeight: FontWeight.w600);
    
const kSubtitleStyle = TextStyle(
  fontSize: 17.0,
);

const kOrangeMarkerColor = Color(0xFFF28608);

var kBoldText = TextStyle(
  color: Colors.black,
  fontWeight: FontWeight.w900,
);

/// accidental creation of the same color listed at the top of this file
final Color kVeryDarkBlue = Color(0xFF30465C);

/// Used for most buttons
final Color kPurple = Color(0xFF93639A);

final Color kLinkText = Color(0xFF0A00FF);